#!/usr/bin/env bash
#
# toggle-nvim — Ctrl+n: toggle nvim in left pane (auto-swap via void)

source "$HOME/drake/nvim-config/bin/left-toggle.sh"

path=$(tmux display-message -p '#{pane_current_path}')

left_toggle "nvim" \
  "bash -c 'source .envrc 2>/dev/null; nvim'" \
  "35%" \
  "$path"
